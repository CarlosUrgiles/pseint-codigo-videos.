//escribir la siguiente expresion en forma de expresion algoritmica (-b+rc(b^2-4*a*c)) / (2*a)
Proceso sin_titulo
	definir a,b,c,resultado Como reales;
	
	escribir "digite el valor de a:";
	leer a;
	escribir "digite el valor de b:";
	leer b;
	escribir "digite el valor de c:";
	leer c;
	resultado<- (-b+rc(b^2-4*a*c)) / (2*a);
	
	escribir "el valor del resultado es:",resultado;
	
FinProceso
